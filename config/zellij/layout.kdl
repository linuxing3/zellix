layout {
    // antigravity风格的AI编辑器布局
    tab name="editor" {
        pane split_direction="vertical" {
            
            // 中部：编辑器 (70%高度)
            pane size="70%" {
                editor_pane
            }
            
            pane size="30%" split_direction="horizontal" {
                // 左侧：AI聊天面板
                pane size="66%" {
                    pane name="ai_chat" borderless=true {
                        command "nu"
                        args "-c" "nu ($env.ZELLIX_MOD + '/ai.nu')"
                    }
                }
                
                // 右侧：终端面板
                pane size="44%" {
                    pane name="terminal" borderless=true {
                        command "zsh"
                        args "-i"
                    }
                }
            }
        }
        
    }
    
    tab name="www" {
        pane borderless=true command="elinks" name="www" {
            args "https://duckduckgo.org"
        }
    }
    
    shell_tab name="shell"
    
    // tab template
    tab_template name="shell_tab" {
        pane split_direction="vertical" {
            pane size="70%" borderless=true command="zsh" name="shell" {
                args "-i"
            }
            pane size="30%" borderless=true command="zsh" name="shell" {
                args "-i"
            }
        }
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
    }
    
    // editor template
    pane_template name="editor_pane" {
        command "nu"
        args "-c" "nu ($env.ZELLIX_PATH + '/helix.nu')"
    }
    
    // default tab
    default_tab_template {
        children
        pane size=1 borderless=true {
            plugin location="compact-bar"
        }
    }
}

